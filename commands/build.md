---
description: Rebuild loop commands from presets
---

# Build Commands

Run: `python ${CLAUDE_PLUGIN_ROOT}/scripts/build.py`

Report: commands generated, user presets loaded from `~/.config/claude-loop/presets.toml`, errors.

Use after adding custom presets or plugin updates.
